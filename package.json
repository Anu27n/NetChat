{
  "name": "netchat-root",
  "private": true,
  "scripts": {
  "install:all": "npm --prefix server install && npm --prefix client install",
  "postinstall": "npm run install:all",
  "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run dev\" \"wait-on tcp:5000 && npm --prefix client run start:client\"",
  "start": "npm run dev",
  "start:all": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.2"
  }
}
