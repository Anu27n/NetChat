{
  "name": "netchat-root",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -k -n server,client -c blue,green \"npm --prefix server run dev\" \"wait-on tcp:5000 && npm --prefix client start\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.2"
  }
}
